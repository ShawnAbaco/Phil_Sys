name: Deploy to InfinityFree via FTP

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: FTP Deploy ALL files
        uses: airvzxf/ftp-deployment-action@latest
        with:
          server: ftpupload.net
          user: if0_41250312
          password: KgYVVdsrD6ymy0n
          local_dir: "./"
          remote_dir: /htdocs/
          delete: "false"
          ftp_secure: "false"
          ftp_port: 21
          max_retries: 30
          net_max_retries: 3
          net_timeout: 60s
          ftp_passive_mode: "true"
          no_symlinks: "true"
          mirror_verbose: "true"  # This enables recursive upload
